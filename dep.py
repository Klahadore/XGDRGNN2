import torch
from torch_geometric.data import HeteroData
import torch_scatter
import torch_sparse
import torch_cluster
import torch_spline_conv
import pyg_lib

print("PyTorch Geometric and its dependencies are installed correctly.")
